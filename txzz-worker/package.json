{
  "name": "txzz-secure-pool-worker",
  "version": "1.3.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test",
    "check": "node --check src/worker.js && node --check src/security.js && npm test",
    "check:worker": "wrangler deploy --dry-run --outdir .wrangler-check"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}
