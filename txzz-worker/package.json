{
  "name": "txzz-secure-pool-worker",
  "version": "1.4.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "node --test test/worker.test.js test/security.test.js",
    "check": "node --check src/worker.js && node --check src/security.js && node --check src/domain/playback.js && node --check src/domain/diagnostics.js && npm test",
    "check:worker": "wrangler deploy --dry-run --outdir .wrangler-check"
  },
  "devDependencies": {
    "wrangler": "4.110.0"
  }
}
